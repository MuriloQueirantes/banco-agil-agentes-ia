[pytest]
testpaths = tests
addopts = -q --strict-markers -m "not integracao"
markers =
    integracao: chama a API do Gemini de verdade (rode com -m integracao)
filterwarnings =
    ignore::DeprecationWarning
